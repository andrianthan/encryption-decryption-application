## Encryption Decryption Application

A simple Python application for encrypting and decrypting files using AES Fernet encryption from the cryptography library. 

## Prerequisites

Before executing the project, make sure you have the following installed:

1. **Python**

2. **cryptography package**
    ```bash
    pip install cryptography
    ```

    or

    ```bash
    pip3 install cryptography
    ```



